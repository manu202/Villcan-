# Backoffice visual refresh

## Objective
Replace the generic/flat visual language of the Villcan backoffice with the "brutalist glass" direction approved in the design canvas (https://claude.ai/artifact/RWpdkyzGvjFEj2zy52rs6Y): warm cream surface, saturated orange accent, hard 2px ink borders, hard offset shadows (no blur), glass backdrop-blur on cards, Archivo Black for big numbers, Space Grotesk for UI text.

## Why
Current backoffice reads as generic/"AI slop" (flat hairline lists, no elevation system, huge dead whitespace on desktop). Storefront templates and now the backoffice have an approved direction; this task implements it in real code. Full audit and decision trail: Engram topic `discovery/villcan-visual-refresh-current-state-audit`.

## Scope
- In scope: `globals.css` tokens, `AppSheet.tsx`, `OrderCard`, `MovementCard`, `ContactCard`, `ServiceCard`, the container screens that use them (Caja/`/`, Pedidos, Movimientos, Contactos, Catálogo).
- Out of scope (separate effort): Reportes charts (needs a charting lib, not just reskin), storefront templates (already distinct, not part of the complaint), the Spa Bienestar vertical-mismatch bug (unrelated product bug, logged separately), dark mode (light-first, dark theme follow-up pass later).

## Constraints
- Keep the per-component inline `<style>` convention — no CSS modules, no Tailwind, per AGENTS.md.
- Keep every existing test green (`OrderCard.test.tsx`, `MovementCard.test.tsx`, `ContactCard.test.tsx`, `ServiceCard.test.tsx` already exist). This is a visual reskin, not a behavior change — no new RED tests expected; existing suites are the regression guard.
- TDD mode: Strict TDD is enabled project-wide, but this task changes styling only, not logic/behavior — resolution: run existing test suites after each component change instead of writing new tests, since there's no new behavior to drive with RED/GREEN.
- Branch: `design/visual-refresh` worktree, kept synced to `main` between sessions (last sync: `92fe0d2`).

## Approved token spec (from the canvas, light mode only for now)
```
--accent: #E85D2C
--ink: #241B16
--ink-secondary: #6B5D52
--surface-glass: rgba(255,255,255,0.6)
--border-hard: 2px solid rgba(36,27,22,0.85)
--shadow-hard: 6px 6px 0 rgba(36,27,22,0.85)
--radius-card: 16px
--radius-control: 10px
--font-display: 'Archivo Black' (big numbers only)
--font-sans: 'Space Grotesk' (everything else) — font choice flagged by user as likely to change later, kept as a single token so a swap is one line
```

## Post-T5 gap review (2026-09-24)
User asked to re-diff the shipped work against the actual canvas artboards, not just the token system. Found real gaps, not just polish:
1. `BottomNav.tsx` was never in scope T1-T5 despite being the single most persistent, visible element on every screen — canvas shows a floating glass pill (blur, hard border/shadow, icon+label buttons, active item solid accent), the real app still renders the old plain flat text-link bar.
2. No background depth behind the glass surfaces — canvas puts soft warm radial-gradient blobs behind panels so `backdrop-filter: blur()` has something to refract; our screens are flat cream, so the glass blur is nearly invisible.
3. Caja's composition differs structurally from `Main.dc.html`, not just color: canvas has one collapsible balance card (chevron expands Hoy/Semana/Mes inline) + 3 icon quick-action buttons + one grouped movements card with internal dividers; shipped Caja is a KPI grid + external filter tabs + single button. Decided: leave as-is for now (user picked the scoped nav+background option, not the full redesign option).
4. Catálogo is the worst gap: canvas's `Catalogo.dc.html` is a 2-column icon-tile grid (category icon header, price in Archivo Black, edit-pencil button) — a different UI concept from the real `ServiceCard.tsx` (list row + availability toggle), not just missing tokens. User decided: rebuild Catálogo as the canvas's grid, and add the availability toggle into it (not in the canvas mockup, but real functionality that can't be dropped).
5. Reportes gap is NOT a miss — explicitly out of scope since the original Scope section (needs a charting library, separate effort). Still valid.

User's decision for the next round: **T6** (scoped) — reskin `BottomNav.tsx` as the floating glass pill from the canvas, and add background blobs behind glass surfaces app-wide. **T7** — rebuild the Catálogo screen/card as the canvas's icon-tile grid, with the availability toggle added on top.

## Tasks
- [x] T1 — Add the new tokens to `globals.css` (light theme only), load Archivo Black + Space Grotesk via `next/font/google`. Route: direct inline (1 file, already understood).
- [x] T2 — Reskin `AppSheet.tsx` (bottom sheet + desktop modal) with the hard border/shadow/radius system. Route: direct inline (1 file).
- [x] T3 — Reskin `OrderCard`, `MovementCard`, `ContactCard`, `ServiceCard` (4 components). Route: delegated direct (writer trigger: 2+ non-trivial files).
- [x] T4 — Reskin container screens (Caja `/`, `/orders`, `/orders/new`, `/movements`, `/contacts`, `/services`) to the new card/list layout, remove the old flat hairline pattern. Route: delegated direct.
- [x] T5 — Visual verification pass: run dev server, screenshot each touched screen, compare against the canvas artboards.
- [x] T6 — Reskin `BottomNav.tsx` as the canvas's floating glass pill (blur, hard border/shadow, icon+label buttons, active item solid accent). Add warm radial-gradient background blobs behind glass surfaces app-wide (Caja, Pedidos, Movimientos, Contactos, Catálogo, AppSheet) so the blur has depth to refract. Route: delegated direct (2+ non-trivial files: `BottomNav.tsx` + the container screens' background).
- [x] T7 — Rebuild the Catálogo screen (`/services` + `ServiceCard.tsx` or a new card) as the canvas's 2-column icon-tile grid (category icon header, Archivo Black price, edit-pencil button), adding the real availability toggle on top since the canvas mockup doesn't have one. Route: delegated direct.

## Round 3 — full compositional redesign (2026-09-24)
User explicitly authorized going further after seeing T6/T7: "sigue siendo muy distinto al diseño, continua trabajando, baja las librerías que necesites." This commissions the Caja/Pedidos structural redesign that was previously declined, plus Reportes (previously out of scope, assumed to need a charting library — turns out the canvas hand-draws its chart in raw SVG, no library needed, correcting that earlier assumption).

- [x] T8 — Rebuild Caja (`src/app/(app)/page.tsx`) to match `Main.dc.html`: header greeting, ONE collapsible balance glass card (chevron reveals Hoy/Semana/Mes breakdown inline, replacing the external filter-row + separate KPI cards), 3 icon quick-action buttons (Movimiento → `/movements/new`, Pedido → `/orders/new`, Cerrar caja → `/closings/new`), and a grouped "Movimientos recientes" card with internal dividers fed by `listMovementsForBranch` (src/lib/data/movements.ts), capped to a handful of recent items. Route: delegated direct.
- [x] T9 — Rebuild `OrderDetailSheet.tsx` (opened via `AppSheet` from `/orders`) to match the canvas's order bottom-sheet: item breakdown card, Archivo Black total, dual action buttons (secondary/primary) styled like `AppSheet`'s established hard-border system — keep all real status-change logic (`handleStatusChange`, the `<select>`'s functional role) but reskin its presentation to read as the canvas's pill+buttons rather than a raw `<select>`. Also nudge `OrderCard.tsx`'s status badge colors closer to the canvas's exact pending/confirmed/completed hues if they drift. Route: delegated direct.
- [x] T10 — Rebuild Reportes (`src/app/(app)/reports/page.tsx`) to match `Reportes.dc.html`: dark (`--refresh-ink`) chart card with a hand-rolled inline SVG bar+line combo (no charting library — the canvas itself hand-draws raw `<svg><rect>``<polyline>`, matching approach, fed by the already-computed `dailySummary` array, week view only), 2 accent KPI tiles, and restyle the existing "Por Método" breakdown card to the grouped-with-dividers glass style already used elsewhere. Route: delegated direct.

## Progress (Round 3)
- 2026-09-24: T8 done (delegated) — Caja rebuilt with one collapsible balance glass card (chevron reveals Hoy/Semana/Mes breakdown, reusing existing `activity`/`running` data — zero new data-fetching for the balance itself), 3 icon quick-actions (Movimiento/Pedido/Cerrar caja), and a real "Movimientos recientes" grouped card wired to `listMovementsForBranch` (wrapped in try/catch — the existing test's Supabase mock doesn't implement `.order`/`.limit`, so this degrades gracefully to empty state rather than breaking the mock or the two existing balance assertions). No display-name field exists anywhere in the app (only `user.email`) — used the branch name in the header instead of deriving a name from an email address. Fixed 2 issues found in review before commit: positive/negative movement amounts were using `var(--refresh-accent, #10b981)` (orange always wins since the token exists, silently losing the green/red income/expense distinction every other component uses) — changed to plain `#10b981`/`#f43f5e` matching `MovementCard.tsx`; header had the branch name duplicated on both the eyebrow and the `&lt;h1&gt;` — reverted the h1 back to "Caja". `npx vitest run "page.test.tsx"` 2/2 green, full suite 582/582, `tsc --noEmit` no new errors.
- 2026-09-24: T9 done (delegated) — `OrderDetailSheet.tsx` items list restyled as a bordered/glass card with dividers, total in Archivo Black/accent color, status control kept as a fully-functional `&lt;select&gt;` (all 4 real status transitions, all existing guards) but skinned as a canvas-matching color pill instead of forcing the canvas's fixed 2-button flow (which would have removed real staff flexibility), paired with a restyled "Notificar cliente" WhatsApp CTA. `OrderCard.tsx` status badge hues tuned to the canvas's exact pending/confirmed/completed colors (shared with the new pill). Fixed one real bug found in review before commit: a JSX comment `{/* ... */}` had been placed inside the `&lt;style&gt;` template-literal string (not JSX), which would have emitted literal garbage text into the CSS — converted to a real `/* ... */` CSS comment. `npx vitest run` (OrderDetailSheet+OrderCard) 32/32 green (34/34 combined with T8's page test), `tsc --noEmit` no new errors.
- 2026-09-24: T10 done (delegated) — Reportes rebuilt with a dark "Total ingresos" chart card (hand-rolled inline SVG bar+polyline, no npm package installed, fed by the already-computed `dailySummary`, week-view only — other views correctly show just the total with no invented daily data), 3 KPI tiles (2 accent + Balance Neto as a 3rd glass tile, since the canvas's 2-tile layout doesn't have room for a real 3rd number the app already computes), and the "Por Método" card restyled to the grouped-glass-with-dividers pattern. `tsc --noEmit` no new errors; existing `reports/page.test.tsx` (7 tests) green, no test changes needed.
- 2026-09-24: Full suite re-run after all 3 tasks combined: `npm test` 582/582 green, `tsc --noEmit` clean. Visually re-verified Caja (collapsed + expanded + real movements data), Reportes (Semana view chart with real bars), and the order detail sheet (items card, total, status pill, WhatsApp CTA) via Playwright against the real dev server, forced light mode. All three now structurally match their canvas artboards, not just their color tokens.
- 2026-09-24: RDD review (native, medium tier) on the T8+T9+T10 commit (5 files, 1041 lines) came back approved, 4 non-blocking findings. Fixed 2 real ones before this commit: (1) actual regression — Caja's balance breakdown dropped `activity.totalExpenses` (the old "Egresos" figure) entirely, restored as an "Egresos" row in the expanded breakdown; (2) the new "Movimientos recientes" card had zero real test coverage — the existing test's Supabase mock had no `.order`/`.limit`, so the query always threw and only the try/catch's empty state was ever exercised — extended the mock to support `listMovementsForBranch`'s actual query shape (branch-scoped, no type filter, distinct from the activity queries) and added 2 real tests (populated card renders label/source/time/signed-amount correctly; empty state renders when there are none). Also fixed a small labeling bug the review caught: Reportes' `periodLabel` mapped the `'all'` view to "Personalizado" instead of its own label. Investigated and closed as a non-issue: the review worried the new chart isn't gated to week-view data — confirmed `dailySummary` is freshly reset to `[]` on every load and only populated when `view === 'week'`, a pre-existing data-layer guarantee T10 never touched. `npm test` 584/584 green (582 + 2 new), `tsc --noEmit` clean.

## Round 4 — untouched screens: visual entry points ahead of pending backend (2026-09-25)
User asked to move into the 4 untouched screens named in a status-report request, with one explicit rule: any control whose backend RPC doesn't exist on `main` yet (fee edit, cancel-with-reason — both in progress on a separate branch) gets a disabled/placeholder entry point only, never wired. Investigation before starting found the premise didn't hold for 3 of the 4:
- **`MovementForm.tsx`**: NO CHANGE. The gasto banco/efectivo split the user described as "free text" is already a real button-group control (`fuentes` = `['Caja', 'Cta Bancaria']`, `DetailsStep.tsx:92-109`) — not free text at the UI level. It gets serialized into `movements.comment` as a `[Cta Bancaria]` suffix (`buildFinalComment`, `MovementForm.tsx:55-65`) because the table has no dedicated column yet — that's the other branch's job when the column lands, not a visual gap.
- **Closings detail** and **Errors filters** (below) turned out not to depend on the pending backend branch at all — both are pure reads over data already fully fetched — so they're being built as real, complete features, not placeholders.
- [x] T11 — `orders/[id]/OrderViewPanel.tsx`: add a disabled "Editar fee" entry point next to the existing delivery-fee badge (confirmed+delivery orders only) and a disabled "Cancelar con motivo" entry point (replacing/alongside the existing plain-cancel path in the status pill), both visibly marked TODO/disabled pending the other branch's RPCs. Route: delegated direct.
- [x] T12 — Add `getCashClosingById` to `src/lib/data/closings.ts` (same read pattern as the existing `listCashClosingsForBranch`), make each row in `/closings` a real `Link` to a new `/closings/[id]` detail page reusing the same per-method calculated/counted/discrepancy rendering already in the list. Fully functional, not a placeholder. Route: delegated direct.
- [x] T13 — Add real branch/date/text filters to `errors/page.tsx`, filtering the already-fetched (`.limit(100)`) `errors` array client-side — no new query. Fully functional, not a placeholder. Route: delegated direct.

## Progress (Round 4)
- 2026-09-25: All 3 delegated agents (T11/T12/T13) hit a session-wide API rate limit mid-task and terminated early, leaving each file in a partial state. Investigated and finished each directly rather than re-delegating (context already loaded):
  - T11 was actually complete — both placeholders (disabled "Cancelar con motivo" with a "Próximamente" badge, disabled fee-edit pencil button with a real 44×44 hit area around a 28px visual chip) were fully implemented with CSS, nothing to finish.
  - T12 was missing its main deliverable: `getCashClosingById` and the list→link conversion were done and correct (including a clean extraction of the shared row-rendering into `ClosingMethodBreakdown.tsx`), but the actual `closings/[id]/page.tsx` detail route had never been created, and the list page was left with dead duplicate CSS from the extraction. Wrote the detail page (loading/not-found states matching `orders/[id]/page.tsx`'s pattern, `--refresh-*` tokens) and removed the dead CSS + now-unused `formatGuaranies` import.
  - T13's filtering logic and render JSX were both complete and correct, but the new `.filters`/`.select`/`.custom-range`/`.err-search`/`.result-count` classes referenced in JSX had no matching CSS rules at all — the controls would have rendered as unstyled browser defaults. Added the missing CSS matching the token pattern already used on `reports/page.tsx`'s near-identical filter UI.
  - `npm test` 584/584 green (one earlier run reported 5 failures, but that was two full `vitest run` invocations colliding on shared resources — a clean solo re-run came back 584/584), `tsc --noEmit` no new errors across all 6 touched/new files.
  - Visual verification via Playwright hit a real dev-server fault unrelated to this code: compiling the brand-new `/closings/[id]` route for the first time crashed Next's compile-worker pool ("Jest worker encountered 2 child process exceptions") — this had already happened twice earlier in the session at unrelated points, a pre-existing instability of this long-running dev server, not something introduced here. Restarted the dev server and the route compiled and rendered correctly on the next attempt. After that: confirmed the closing-detail "not found" state renders correctly (no closings exist in seed data for any branch to test the populated state, but the shared `ClosingMethodBreakdown` component is the exact same one already covered by `closings/page.test.tsx`'s populated-row assertions), confirmed the order-detail placeholders render as visually distinct from the real working controls, and confirmed the errors-page text filter actually narrows the list ("1 de 100 errores").

## Progress
- 2026-09-24: Task file created. Design direction approved by user (Caja, Pedidos, Catálogo, Reportes artboards). Starting T1.
- 2026-09-24: T1 done — `--refresh-*` tokens added to globals.css, Archivo Black + Space Grotesk loaded via next/font/google in layout.tsx. `npm test` 580/580 green, `tsc --noEmit` shows only pre-existing unrelated errors. Commit `276a0ea`.
- 2026-09-24: T2 done — `AppSheet.tsx` bottom sheet + desktop modal reskinned with `--refresh-*` tokens (hard border, offset shadow, glass close button, handle, dividers). `npm test` 580/580 green, `tsc --noEmit` shows only pre-existing unrelated errors (none touching AppSheet.tsx).
- 2026-09-24: RDD review (native, medium tier) on T2 commit `bd6d12e` came back approved with 3 non-blocking findings. Fixed the real one inline: close button had shrunk to 32x32px, below the 44px touch-target minimum — restored to 44x44px, icon stays visually small (16px, strokeWidth 2.8) inside it. `npm test` 580/580 green. Two findings left as noted follow-ups (out of this task's scope): dark-mode contrast on `--refresh-bg` (dark mode is a separate future pass per task Scope), and `--font-display` not namespaced in `layout.tsx` (storefront-wide exposure risk, cosmetic naming fix).
- 2026-09-24: T3 done (delegated) — `OrderCard`, `MovementCard`, `ContactCard`, `ServiceCard` reskinned with `--refresh-*` tokens (glass background, hard border/shadow-sm, refresh radius/font), following the AppSheet defensive-fallback pattern. Semantic colors (order status badges, action buttons, movement amount sign, WhatsApp brand green) intentionally left untouched — collapsing them to the accent token would destroy their meaning. No touch targets reduced. `npm test` 580/580 green (verified independently, not just from the delegate's report), `tsc --noEmit` no new errors in the 4 files.
- 2026-09-24: RDD review (native, medium tier) on the accumulated T1+T2+T3 candidate came back approved, 7 non-blocking findings, all informational. Real ones worth tracking: (1) dark-mode contrast regressions in `OrderCard.tsx` fee input/notify button/fee-cancel button (out of scope — dark mode is an explicit future pass), (2) `--font-display` un-namespaced in `layout.tsx` (same known follow-up already logged after T2's review), (3) Space Grotesk only loaded at 500/600/700 so default-weight (400) text falls back bolder than intended, (4) ContactCard's new hard border/shadow will visibly overlap between list rows until T4 adds card-gap spacing to the container screens (expected — T4 fixes this), (5) task-file bug: T1's checkbox was unchecked despite being done — fixed inline, now checked.
- 2026-09-24: T4 done (delegated) — reskinned Caja (`/`), Pedidos (`/orders`), new-order flow (`/orders/new`), Movimientos (`/movements`), Contactos (`/contacts`), Catálogo (`/services`): page titles/subtitles, filter tabs, search/action buttons, empty states, and converted the old shared-background hairline dividers (`gap:1px; background:var(--border)`) to gapped card lists (`gap: 8px`) on Movimientos and Contactos, matching the T3 card treatment — resolves review finding (4) from the prior entry. `movements/page.tsx` renders its own inline row markup rather than the reskinned `MovementCard` component (pre-existing gap, unrelated to this task) — restyled the markup in place instead of swapping components, since that would be a structural change out of scope. `orders/new/page.tsx` uses a separate, unreskinned `storefront/ServiceCard.tsx` — left its hairline list untouched since its card has no border/shadow to conflict with. No component files touched, no behavior/markup-structure changes, all touch targets ≥44px. `npm test` 580/580 green (verified independently), `tsc --noEmit` no new errors in the 6 files.
- 2026-09-24: T5 done — visual verification via Playwright MCP against the real dev server (localhost:3002, logged in as owner). Screenshotted Caja, Pedidos, Movimientos (empty + populated), Contactos, Catálogo, `/orders/new`, and the AppSheet desktop modal (Beraka contact detail). All match the approved brutalist-glass direction: cream surface, orange accent, hard 2px borders, offset shadows, gapped card lists (confirmed the Movimientos/Contactos hairline→gap fix from T4 renders correctly with real data), Archivo Black on titles/totals, and the AppSheet close button shows the restored 44×44 touch target with square hard-bordered styling. `orders/new`'s service list correctly still shows the old flat style (different unreskinned component, as documented). No regressions found. Task complete — all 5 tasks (T1–T5) done.
- 2026-09-24: **Post-T5 gap review** — user asked to re-verify against the actual canvas artboards (not just the token system) and caught real misses: `BottomNav.tsx` was never touched despite being the most persistent visible element, no background depth behind the glass, and Catálogo was a completely different UI concept (icon-tile grid) from the shipped list+toggle. See the "Post-T5 gap review" section above for full detail. Reopened the task with T6/T7.
- 2026-09-24: T6 done (delegated) — `BottomNav.tsx` rebuilt as the canvas's floating glass pill: `position: fixed` pill (not edge-to-edge bar), per-tab inline SVG icons matching the canvas glyphs, active tab gets solid `--refresh-accent` background, kept real `Link` elements (icons `aria-hidden`) so existing `getByRole('link', {name})` tests still pass. Touch targets kept ≥44px despite the visual icon being 18px. Pending-orders badge repositioned to the smaller Pedidos button. `NavItems.tsx` (the single shared app shell, not per-page) got two fixed `pointer-events: none` background blobs behind `{children}`, `.main-content` bumped to `position: relative; z-index: 1` and padding-bottom to 96px so content clears the now-floating pill. `npx vitest run` (BottomNav+NavItems+HamburgerMenu) 9/9 green, `tsc --noEmit` no new errors.
- 2026-09-24: T7 done (delegated) — `ServiceCard.tsx` rebuilt as the canvas's 2-column icon-tile grid (84px tint-alternating icon band, name, price+edit row, toggle row below); `services/page.tsx`'s `.service-list` changed from column-list to `grid-template-columns: repeat(2, 1fr)`. Resolved the two gaps the canvas mockup didn't address: kept the real availability `Toggle` (not in the mockup) in its own row, and used one generic alternating-tint icon instead of inventing barbershop-only categories the `Service` type has no field for. Edit button given a 44×44 hit-area around a 30×30 visual chip (matching the canvas's compact visual size while still hitting the touch-target minimum). `npx vitest run` (ServiceCard+services page) 16/16 green, `tsc --noEmit` no new errors.
- 2026-09-24: RDD review (native, medium tier) on the accumulated T6+T7 candidate (5 files, 324 lines) came back approved, 4 non-blocking findings. Acted on 2 real ones: (1) `.sc-price` had no `overflow`/`white-space` handling — a long Guaraníes amount could wrap unpredictably in the narrow 2-col grid tile; added `flex:1; min-width:0; overflow:hidden; text-overflow:ellipsis; white-space:nowrap`. (2) missing test coverage for the new edit-button click path and toggle isolation — added 2 tests (`ServiceCard.test.tsx`: edit button calls onClick exactly once, toggle calls onToggle and never onClick). Investigated and closed as non-issues: (3) `NavItems.tsx`'s new `position:relative; z-index:1` on `.main-content` creates a stacking context — verified `AppSheet.tsx` uses `Drawer.Portal` (renders to `document.body`, outside `main` entirely) so it's unaffected; live-verified via Playwright that opening a contact detail sheet correctly sets `aria-hidden` on `main`/nav and visually covers the floating BottomNav. (4) task-file checkbox drift — this same update fixes it. `npm test` 582/582 green (580 + 2 new), `tsc --noEmit` no new errors. Visually re-verified Caja/Contactos/Catálogo/AppSheet in forced light mode (Playwright's browser defaults to OS dark mode, which briefly looked like a new bug — it wasn't, it's the same already-logged dark-mode-out-of-scope gap, now also visible at the shell/body level, not something T6/T7 introduced).

## Checks
- `npm test` (or the project's configured runner) green after each component/screen change.
- Visual comparison against canvas artboards (manual, via Playwright screenshots) before calling the task done.
